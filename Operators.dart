void main(){
    //Operators of  dart

    //1.Arithematic Operator
    int a = 10;
    int b = 4;
    int c = a+b;
    print(a.toString()+ " + "+b.toString() + " is "+ c.toString());

    int d = a-b;
    print(a.toString()+ " + "+b.toString() + " is "+ d.toString());
    int e = a*b;
    print(a.toString()+ " + "+b.toString() + " is "+ e.toString());
    double f = a/b;
    print(a.toString()+ " + "+b.toString() + " is "+ f.toString());

    //other Arithmatic operators.
    /* 
    1.Add
    2.sub
    3.Div
    4.Multi
    5.Truncating division operator ~/ converts decimal into int
    6.modulous % to get reminder
    ++increement
    --decreement
  */
  // int d = (a++)+(++a)+(b--)+(--b)+(a++)+(b--)+(++a)-(a++)+(--b);
  // print(d.toString());
  }