import 'dart:io';

void main() {
  // Task 1

  // stdout.write("Enter your name ");
  // var name = stdin.readLineSync();
  // stdout.write("Enter product title ");
  // var product_title = stdin.readLineSync();
  // stdout.write("Enter product Quantity ");
  // var Qntty = int.parse(stdin.readLineSync()!);

  // print("${name} ordered ${Qntty} ${product_title} ");

// //  Task 2 & Task 3
//   print("Enter two numbers ");
//   var a = int.parse(stdin.readLineSync()!);
//   var b = int.parse(stdin.readLineSync()!);
//   // adding  of two numbers
//   int c = a + b;
//   print("The sum of ${a} and ${b} is ${c}");

//   // subtraction of  two numbers
//   c = a - b;
//   print("The sub of ${a} and ${b} is ${c}");

//   // multiplication of  two numbers
//   c = a * b;
//   print("The sub of ${a} and ${b} is ${c}");

// // division of  two numbers
//   double d = (a / b);
//   print("The sub of ${a} and ${b} is ${d}");

// // Task 4
  // stdout.write("How many tickets do you want? ");
  // var a = int.parse(stdin.readLineSync()!);
  // int Ticketprice = 900;
  // int total_price = a * Ticketprice;
  // print(
  //      "Ticket price is ${Ticketprice} \n you've purchased ${a} Tickets of The Avengers Your Bill is ${total_price}");

// Task 5

  // stdout.write("Enter temp in celsius ");
  // var c = double.parse(stdin.readLineSync()!);
  // double F = (c * 9 / 5) + 32;
  // print("${c} Celsius is equal to ${F} Fahrenhiet .");

  //Task 6
  // stdout.write("What do you want in clothes  sir ?");
  // var item1 = stdin.readLineSync();

  // stdout.write("how much ?");
  // var Quantity = int.parse(stdin.readLineSync()!);

  // stdout.write("What do u want something in shoes ?");
  // var item2 = stdin.readLineSync();

  // stdout.write("How much?");
  // var Qntty = int.parse(stdin.readLineSync()!);

  // double Item1_price = Quantity * 1500;
  // double Item2_price = Qntty * 3500;
  // double Total_price = Item1_price + Item2_price;

  // print(
  //     " SHOPPING CART \nPrice of ${item1} is ${Item1_price}Rs\nQuantity of ${item1} is ${Quantity} \nPrice of ${item2} is ${Item2_price} Rs\nQuantity of ${item2} is ${Qntty} \nTotal Price of Both Items is ${Total_price} Rs");

  // Task7
  // stdout.write("Enter total marks of IT subject ");
  // var total_marks = int.parse(stdin.readLineSync()!);
  // stdout.write("Enter marks you secured in IT . ");
  // var marks = int.parse(stdin.readLineSync()!);
  // double Percentage = (marks / total_marks) * 100;
  // print("your percentage is ${Percentage}%");

  // Task8
  // stdout.write("Enter Dollars ");
  // var D = int.parse(stdin.readLineSync()!);
  // stdout.write("Enter Saudi Riyal ");
  // var R = int.parse(stdin.readLineSync()!);
  // double DtoPKR = D * 178.50;
  // double RtoPKR = R * 47.58;
  // double Total_Amount = DtoPKR + RtoPKR;
  // print(
  //     "${D} dollars are ${DtoPKR} Pakistani Rs...\n${R} Saudi Riyals are ${RtoPKR} Pakistani Rs...\nThe total of Dollar and SR in PKR is ${Total_Amount}Rs");

  // Task 9
  // stdout.write("Enter Hours of job you work here _");
  // var hours = double.parse(stdin.readLineSync()!);
  // stdout.write("Enter rate per hour _");
  // var rate_perhour = double.parse(stdin.readLineSync()!);
  // double pay = hours * rate_perhour;
  // print(
  //     "you work ${hours} hours. \nyour rate/hour is ${rate_perhour}Rs \nyour Total Pay is ${pay}Rs");
}
